---
- name: debug_excrcise4
  hosts: vyos01
  gather_facts: false
  
  tasks:
    - name: show commands
      vyos_command:
        commands: "show vrrp"
      register: result

    - name: vrrp state
      debug:
        var: result.stdout_lines

